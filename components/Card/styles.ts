import styled from 'styled-components';

export const Container = styled.div`
  width: 80%;
  height: 'auto';
  background:#fff;
  margin: 3.6rem ;
  padding: .8rem;
`;

export const Text = styled.p`
  font-size: 1.2rem;
  font-weight: normal;
  text-align: center
`;